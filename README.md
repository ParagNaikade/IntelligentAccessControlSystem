# IntelligentAccessControlSystem
Build an AI-driven document access-control platform that classifies uploaded docs, detects sensitive info (PII, credentials, secrets), produces an access policy (allow/deny/restrict) using NER + Azure OpenAI, enforces RBAC via Azure AD, and logs everything for audit/alerts — MVP in ~3 weeks.
